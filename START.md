# Solo 跨宿主使用参考

优先通过宿主的 Skill 选择器启动 Solo：

- Codex：调用 `$solo`
- Claude Code：调用 `/solo`
- Qwen Code：调用 `/solo`

当前维护以上三个编码宿主。其他宿主后续按需求适配；通用 `dist/portable/solo.zip` 仅作为分发资源，不代表已验证兼容。普通聊天模式降级入口已移除。

首次安装及后续更新使用同一命令：`python3 /Users/monkey/Documents/kit/workspace/solo/installers/install.py --platform all`。它从 core 与平台配置构建完整产物，再同步个人技能目录；修改 core 后需要重新运行。Codex 使用一层目录链接，其他两个宿主默认真实副本；不再维护 adapters 下的公共资源链接外壳。

裸调用先介绍完整步骤和简易/平衡/完整模式；只问未知的项目目录和需求/附件，不强制选择终点。已有项目展示真实进度和下一步。目标明确后默认guided，从需求范围确认开始，不直接编码。

## 本次操作

- 操作：初始化
- 项目目录：未提供时请询问；已在确定的项目内则不重复询问
- 本次成果（可选）：不填则新需求推荐功能可运行，并随范围确认；也可先出方案、上线前可验收或上线交付
- 流程模式（可选）：自动推荐简易 / 平衡 / 完整
- 交互模式：guided

## 需求背景

在引用本文件时，可以把需求背景直接写在消息中，也可以填写到这里。

## 输入材料

可以继续引用或上传图片、PDF、Word、接口示例、数据文件、本地文件和在线文档。

## 特别约束

请说明技术栈、兼容性、发布时间、不可修改范围、数据安全要求或必须沿用的方案。

## 跨宿主继续

如果项目已有 `.ai-delivery/`，必须先读取 `.ai-delivery/state.json`、`AI/output/00 交付工作台.md`、相关产物和当前平台的项目指令文件。以其中最新 `revision` 为准，不得根据聊天记忆重建审批或测试结果。

## 执行要求

1. 展示对背景、目标、范围、事实、假设和开放问题的理解。
2. 列出完整14步及模式对应关系，理解需求后推荐中文模式、成果终点和本次编号路线，说明合并/按需/暂不执行项及原因；不把步骤编号当作必填问卷。
3. 在目标项目中创建或恢复 `.ai-delivery/`、`AI/input` 和 `AI/output`。
4. 生成当前阶段的具体产物并执行结构检查。
5. guided 模式下，在关键语义关口等待确认并合并次要问题。
6. 区分宿主平台和实际模型，并在使用某项工具前确认相关运行能力。
7. 每次回复说明当前修订号、平台和模型、产物、验证证据与下一项决定。
8. 状态发生变化后写入跨宿主交接记录。

完整步骤与模式表见 [使用说明](README.md#完整步骤模式和本次终点)。进入当前阶段前说明工作、材料和产物；到选定终点暂停，不把后续阶段标完成，不因选择上线自动执行生产操作。
